# 実装タスク: 高度な検索機能とサイドペイン詳細表示

- [x] **1. 準備と基盤整備**
    - [x] `task.md` の作成
    - [x] 言語リソース (`ja.json`, `en.json`) に必要な文言を追加
- [x] **2. 依存関係解析と検索エンジンの強化**
    - [x] `useSearch.ts` のリファクタリングと検索精度の向上
    - [x] `useDependencyIndex.ts` フックの作成 (検索とサイドペインで共有するため)
- [x] **3. サイドドロワーと計算式ハイライターの実装**
    - [x] `FormulaHighlighter.tsx` の作成 (シンタックスハイライト、自動スクロール)
    - [x] `SideDrawer.tsx` の作成 (スライドアニメーション、依存関係リンク)
- [x] **4. 画面遷移と URL パラメータ制御**
    - [x] `App.tsx` で `window.location.search` を検知するロジックの実装
    - [x] 検索結果クリック時に URL パラメータを付与して遷移するよう `SearchResultsList.tsx` を修正
- [x] **5. UI の統合とブラッシュアップ**
    - [x] `DetailView.tsx` への `SideDrawer` の統合
    - [x] サイドドロワー展開時のメイン画面バッジの強調表示 (Pill コンポーネントの修正)
    - [x] 全体的なアニメーションとレスポンシブ対応の調整
- [x] **6. 検証とドキュメント作成**
    - [x] 動作確認 (検索 -> 遷移 -> 自動展開 -> ドリルダウン)
    - [x] `walkthrough.md` の作成
